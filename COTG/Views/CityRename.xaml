<ContentDialog
    x:Class="COTG.Views.CityRename"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:COTG.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			   xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
			   mc:Ignorable="d"
    Title="Name (try to avoid '-')"
    PrimaryButtonText="Apply"
    CloseButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    BorderBrush="{ThemeResource SystemControlBackgroundBaseLowRevealBorderBrush}"
    >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" x:Name="suggested" Header="Suggested" x:FieldModifier="public" />
        <TextBox Grid.Row="1" x:Name="name" Header="Current" x:FieldModifier="public"   />
		<ToggleSwitch Grid.Row="2" IsOn="{x:Bind local:CityRename.useSuggested, Mode=TwoWay}" OnContent="Suggested" OffContent="Current"/>
		<controls:WrapPanel Grid.Row="3">
			<ToggleButton x:Name="hub" Content="Hub"/> 
			<ToggleButton x:Name="vanqs" Content="Vanq"/> 
			<ToggleButton x:Name="rt" Content="RT"/> 
			<ToggleButton x:Name="priestess" Content="Priestess"/> 
			<ToggleButton x:Name="prae" Content="Praetor"/> 
			<ToggleButton x:Name="horse" Content="Horse"/> 
			<ToggleButton x:Name="arb" Content="Arb"/> 
			<ToggleButton x:Name="scorp" Content="Scorp"/> 
			<ToggleButton x:Name="sorc" Content="Sorc"/> 
			<ToggleButton x:Name="druid" Content="Druid"/> 
			<ToggleButton x:Name="galley" Content="Galley"/> 
			<ToggleButton x:Name="stinger" Content="Stinger"/> 
			<ToggleButton x:Name="warship" Content="Warship"/> 
			<ToggleButton x:Name="shipping" Content="Shipping"/> 
		</controls:WrapPanel>
		<controls:WrapPanel Grid.Row="4">
		<ToggleSwitch Header="Tags" IsOn="{x:Bind local:CityRename.applyTags, Mode=TwoWay}" OnContent="Apply" OffContent="Leave"/>
		<ToggleSwitch Header="Hub" IsOn="{x:Bind local:CityRename.setHub, Mode=TwoWay}" OnContent="Set" OffContent="Leave"/>
		</controls:WrapPanel>
    </Grid>
</ContentDialog>
