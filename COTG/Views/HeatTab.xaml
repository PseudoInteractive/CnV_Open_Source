<views:UserTab
    x:Class="COTG.Views.HeatTab"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:COTG.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	 xmlns:wuxdata="using:Windows.UI.Xaml.Data"
	xmlns:win="using:Windows.UI.Xaml.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
	Tag="Heat"
    d:DesignWidth="400">

	<Grid >
		<Grid.Resources>
			
		</Grid.Resources>
		
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="*" />
			<RowDefinition Height="auto"/>
		</Grid.RowDefinitions>
		<win:TreeView x:Name="tree" Grid.Row="1"  x:FieldModifier="public"  SelectionMode="Multiple"  IsTabStop="True" TabFocusNavigation="Cycle" ItemsSource="{x:Bind records,Mode=OneWay}" 	PointerEntered="snapshots_PointerEntered" GotFocus="List_GotFocus" ItemInvoked="tree_ItemInvoked"
						KeyDown="tree_KeyDown"  >
			<win:TreeView.ItemTemplate>
				<DataTemplate x:DataType="views:WorldRecord" >
					<win:TreeViewItem ItemsSource="{x:Bind children}"
                               Content="{x:Bind tStr}"/>
					
				</DataTemplate>
			</win:TreeView.ItemTemplate>
		</win:TreeView>
		
		
		<TextBlock Text="World Snapshots:" VerticalAlignment="Center" HorizontalAlignment="Center" IsColorFontEnabled="False" FocusVisualPrimaryThickness="3,2,2,2" IsHitTestVisible="False"  />
		
		<Button Click="Now_Click" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" >
			<TextBlock Text="Clear" HorizontalAlignment="Center" FontSize="14" />
		</Button>
	</Grid>
</views:UserTab>
