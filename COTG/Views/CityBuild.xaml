<UserControl
    x:Class="COTG.Views.CityBuild"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:COTG.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	
	mc:Ignorable="d">

	<GridView x:Name="gridView" SelectionMode="None" IsItemClickEnabled="True" ItemClick="ItemClick" Height="Auto"  >
		<GridView.ItemTemplate>
			<DataTemplate x:DataType="views:BuildMenuItem">
				<StackPanel Orientation="Vertical" ToolTipService.ToolTip="{x:Bind toolTip}">
					<Canvas Width="64" Height="64" Background="{x:Bind brush}" />
					<TextBlock Text="{x:Bind header}"  Style="{StaticResource CenterAlign}" FontSize="10" VerticalAlignment="Bottom" TextWrapping="WrapWholeWords" />
				</StackPanel>
			</DataTemplate>
		</GridView.ItemTemplate>
		<GridView.ItemsPanel>
			<ItemsPanelTemplate>
				<ItemsWrapGrid MaximumRowsOrColumns="4"
                              Orientation="Horizontal" />
			</ItemsPanelTemplate>
		</GridView.ItemsPanel>
	</GridView>
</UserControl>
