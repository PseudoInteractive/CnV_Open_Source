<ContentDialog
    x:Class="COTG.Views.DateTimePicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:COTG.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Please select Date and Time"
    PrimaryButtonText="Okay"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="32"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="80"/>
            
        </Grid.ColumnDefinitions>
        <CalendarView x:Name="date" SelectionMode="Single" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center" SelectedDatesChanged="date_SelectedDatesChanged"/>
        <TimePicker Grid.Row="1" ClockIdentifier="24HourClock" Time="{x:Bind time,Mode=TwoWay}"/>
        <TextBox PlaceholderText="Sec" Grid.Row="1" Grid.Column="1" Text="{x:Bind seconds, Mode=TwoWay, Converter={StaticResource IntConverter}}"/>
    </Grid>
</ContentDialog>
