<ContentDialog
    x:Class="COTG.Views.ContinentTagFilter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:COTG.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:tk="using:Microsoft.Toolkit.Uwp.UI.Controls"
	Title="Filter"
    PrimaryButtonText="Okay"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">
    <StackPanel>
		<TextBlock Text="Continents" />
		<Border
                    Style="{StaticResource BorderReveal}">
			<tk:WrapPanel x:Name="continentsPanel" x:FieldModifier="public" />
		</Border>
		<TextBlock Text="Tags" />
		<Border
                    Style="{StaticResource BorderReveal}">
			<tk:WrapPanel x:Name="tagsPanel" x:FieldModifier="public" />
		</Border>
		
	</StackPanel>
</ContentDialog>
