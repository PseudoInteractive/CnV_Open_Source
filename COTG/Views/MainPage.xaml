<Page Background="{StaticResource Acryllic}" xmlns:my="using:Microsoft.UI.Xaml.Controls" 
    x:Class="COTG.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">
	<Grid x:Name="ContentArea" Background="{StaticResource Acryllic}" Margin="360,0,0,0" >
       <CommandBar x:Name="Commands"  >
            <AppBarButton Content="Hello">
                <AppBarButton.Command>
                    <XamlUICommand/>
                </AppBarButton.Command>
                <AppBarButton.Icon>
                    <SymbolIcon/>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Content="Hi" Icon="MailForward">
                <AppBarButton.Command>
                    <StandardUICommand/>
                </AppBarButton.Command>
            </AppBarButton>
        </CommandBar>

        <Hub x:Name="hub" Margin="960,0,0,0" />
    </Grid>
</Page>
