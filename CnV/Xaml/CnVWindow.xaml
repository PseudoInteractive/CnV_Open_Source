<Window
    x:Class="CnV.CnVWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cnv="using:CnV"
    xmlns:views="using:CnV.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid  Background="{x:Null}">
			<Grid.RowDefinitions>
				<RowDefinition Height="auto"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

			<Grid x:Name="AppTitleBar" Margin="0,0,120,0" Background="{x:Null}">
    		<Image  x:Name="titleImage" x:FieldModifier="public"
					Width="24"
					Height="24"
	    			   HorizontalAlignment="Left" 
                       VerticalAlignment="Center"
                       Margin="8,0"/>
				<TextBlock x:Name="titleText" x:FieldModifier="public" 
                       TextWrapping="NoWrap"
						   
                       Style="{StaticResource BaseTextBlockStyle}" 
                       VerticalAlignment="Center"
                       Margin="48,0,0,0"/>
			</Grid>

	    	<views:ShellPage x:Name="shellPage" x:FieldModifier="public"  Grid.Row="1" />
	
	</Grid>
</Window>
