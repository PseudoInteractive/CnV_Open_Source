<UserControl
    x:Class="CnV.CityStats"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cnv="using:CnV"
    xmlns:tk="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
		<tk:WrapPanel Orientation="Horizontal">
			<TextBlock Text="Hello There!" />
			<TextBlock Text="{x:Bind city.stats.storage.wood, Mode=OneWay}" />
			<NumberBox Value="{x:Bind city.stats.storage.stone, Mode=OneWay}" Header="Stone Storage" />
			<NumberBox Value="{x:Bind city.woodStorage, Mode=OneWay}" Header="Wood Storage" />

		</tk:WrapPanel>
	</Grid>
</UserControl>
