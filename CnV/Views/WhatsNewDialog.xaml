<ContentDialog
    x:Class="CnV.Views.WhatsNewDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
	xmlns:cnv="using:CnV"
    Title="Whats New"
	Opened="ContentDialog_Opened"
    PrimaryButtonText="Done"
	
	 mc:Ignorable="d">

	<Border Style="{StaticResource BorderReveal}" Margin="16" Padding="8" >
		<ScrollViewer x:Name="scrollView" x:FieldModifier="public" >
		        <controls:MarkdownTextBlock x:Name="fixesText" x:FieldModifier="public" WrapCodeBlock="True"
                                  Margin="6" Background="{x:Null}" TextWrapping="WrapWholeWords"
											LinkClicked="{x:Bind cnv:Note.MarkDownLinkClicked}"
                                   />
		
	</ScrollViewer>
	</Border>
</ContentDialog>
