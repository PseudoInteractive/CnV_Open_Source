<cnv:DialogG
    x:Class="CnV.SendTroops"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cnv="using:CnV"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	Width="620"
	xmlns:tk="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition  />
			<RowDefinition />
		</Grid.RowDefinitions>
		<Button Grid.Row="1" Style="{StaticResource ButtonMedium}" Content="Send" Click="SendTroopsClick" HorizontalAlignment="Right"/>


		<ListView x:Name="troopList"  Grid.Row="0"  ItemsSource="{x:Bind troopItems,Mode=OneWay}">
		<ListView.ItemTemplate >
			<DataTemplate  x:DataType="cnv:SendTroopItem" >
				<Grid  Height="Auto" Margin="1"  HorizontalAlignment="Stretch" >

					<Grid.ColumnDefinitions>
						<ColumnDefinition Width ="auto"  />

						<ColumnDefinition Width ="*" />
					</Grid.ColumnDefinitions>

					<Image Grid.Column="0" Source="{x:Bind image}" Margin="1"  />
					<Grid  Grid.Column="1" >
						<Grid.RowDefinitions>
							<RowDefinition Height = "40" />
							<RowDefinition />
						</Grid.RowDefinitions>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="80"  />
							<ColumnDefinition Width="* "/>
						</Grid.ColumnDefinitions>
						<TextBlock Grid.Column="0" Text="{x:Bind info.tn}" Margin="8,0,8,0" Style="{StaticResource TextBlockMedium}" TextWrapping="Wrap" TextAlignment="Center" MaxLines="3"  VerticalAlignment="Center" />
						<TextBlock Grid.Column="1" Text="{x:Bind info.dsc}"  Margin="8,0,2,0" Style="{StaticResource TextBlockSmall}" TextWrapping="Wrap"  MaxLines="4" TextAlignment="Center"   VerticalAlignment="Center" />
						<Grid Grid.Row="1" Grid.ColumnSpan="2">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*"  />
								<ColumnDefinition Width="auto "/>
								<ColumnDefinition Width="auto "/>
							</Grid.ColumnDefinitions>
							
							<TextBlock Grid.Column="1" Text="{x:Bind troopsHome,Mode=OneWay}" Style="{StaticResource TextBlockMedium}" VerticalAlignment="Center"/>
							<StackPanel Orientation="Horizontal"   Grid.Column="2"  >
								<NumberBox  Style="{StaticResource NumberBoxMedium}" Value="{x:Bind count,Mode=TwoWay}" Width="120"   Margin="8,0,8,0" VerticalAlignment="Center" />
								

							</StackPanel>
						</Grid>

					</Grid>
				</Grid>


			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
	</Grid>
</cnv:DialogG>
