<UserControl
    x:Class="CnV.MessageBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CnV"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
IsTabStop="True"

	TabFocusNavigation="Cycle"
    mc:Ignorable="d">

    <Grid  MinWidth="200" TabFocusNavigation="Cycle" IsTabStop="True" BorderThickness="1" Padding="4" 
		   Background="{StaticResource ApplicationPageBackgroundThemeBrush}"
		   CornerRadius="4">
		<Grid.RowDefinitions >
			<RowDefinition />
			<RowDefinition MinHeight="60 "/>
			<RowDefinition />
			<RowDefinition />
		</Grid.RowDefinitions>
		<TextBlock x:Name="title" Style="{StaticResource TextBlockMedium}" x:FieldModifier="internal" />
		<Image x:Name="image" Grid.Row="1"  x:FieldModifier="internal"
			   Stretch="None" Margin="4,8,4,8" />
		<Border Grid.Row="1"  
				Background="#6000" Visibility="{x:Bind message.Visibility,Mode=OneWay}"
				BorderBrush="{StaticResource SystemControlHighlightAccentRevealBorderBrush}" BorderThickness="1" Padding="8" Margin="0,0,24,0" HorizontalAlignment="Right" VerticalAlignment="Center"> 
		
			<TextBlock x:Name="message"  Style="{StaticResource TextBlockMedium}"  x:FieldModifier="internal" TextWrapping="Wrap" MaxLines="8" />
		</Border>
		<TextBlock x:Name="footer" Grid.Row="2"  Style="{StaticResource TextBlockMedium}"  x:FieldModifier="internal" TextWrapping="Wrap" MaxLines="8" />
		<Button x:Name ="button" TabIndex="1" IsTabStop="True"  Grid.Row="3" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="2" Style="{StaticResource ButtonMedium}"  x:FieldModifier="internal" />
	</Grid>
</UserControl>
